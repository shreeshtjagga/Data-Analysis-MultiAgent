# DataPulse — Python dependencies
# Install with: pip install -r requirements.txt
# IMPORTANT: Versions are pinned to avoid breaking changes from major upgrades.
# pandas 3.x and plotly 6.x have API breaking changes — keep these caps.

# ─── Data processing ──────────────────────────────────────────────────────────
pandas>=2.0.0,<3.0.0
numpy>=1.26.0,<3.0.0
scipy>=1.13.0
openpyxl>=3.1.2
xlrd>=2.0.1
pyarrow>=16.0.0

# ─── Visualisation ────────────────────────────────────────────────────────────
plotly>=5.22.0,<6.0.0

# ─── API framework ────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn>=0.30.0
python-multipart>=0.0.9
orjson>=3.9.0        # fast JSON — NaN/Inf → null natively, ~10x stdlib speed

# ─── Database ─────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.30
asyncpg>=0.29.0

# ─── Cache (Upstash Redis — KEPT, Supabase has no Redis equivalent) ───────────
redis>=5.0.0

# ─── Auth — Supabase (replaces python-jose, passlib, google-auth) ─────────────
supabase>=2.4.0            # Supabase Python client for auth and DB

# REMOVED (with reasons):
# passlib[bcrypt]    → REMOVED: Supabase handles password hashing internally
# bcrypt             → REMOVED: same reason, no longer store hashed passwords
# python-jose        → REMOVED: Supabase manages JWT signing/verification
# google-auth        → REMOVED: Supabase verifies Google tokens via Dashboard config

# ─── Validation ───────────────────────────────────────────────────────────────
pydantic>=2.7.0
email-validator>=2.1.0.post1

# ─── LLM ──────────────────────────────────────────────────────────────────────
groq>=0.9.0

# ─── Config ───────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
requests>=2.31.0

# ─── RAG ──────────────────────────────────────────────────────────────────────
pinecone>=5.0.0    # serverless vector DB + inference API (zero local ML)
